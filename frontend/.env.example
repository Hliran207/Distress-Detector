# API base URL as seen from the **browser** (not from inside Docker).
# Local Vite dev with API on port 8000:
# VITE_API_BASE_URL=http://localhost:8000

# Docker Compose (backend published as host port 8001):
VITE_API_BASE_URL=http://localhost:8001
